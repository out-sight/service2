spring:
  cloud:
    config:
      uri: ${config.server:http://localhost:8888}
      username: vind
      password: vind      
  application:
    name: service2
  profiles:
    active:
      - dev